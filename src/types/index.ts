export type ColorKey = 'background' | 'text' | 'primary' | 'secondary' | 'accent';

export interface ColorScheme {
  background: string;
  text: string;
  primary: string;
  secondary: string;
  accent: string;
}